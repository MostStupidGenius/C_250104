# include <stdio.h>

int main() {
	// 메인 함수는 각 프로젝트, 각 솔루션마다
	// 반드시 하나만 있어야 하며
	// 프로그램을 실행하면 main함수를 찾아간다.

	// 함수는 전달된 값을 받아서
	// 그 값을 어떤 연산을 처리한 뒤
	// 되돌려주는 역할을 한다.

	// 메인 함수의 return 0;은
	// 메인 함수가 정상 종료되었음을 의미한다.
	
	return 0;
}